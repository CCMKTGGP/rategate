import type { Metadata } from "next";
import Script from "next/script";
import "./globals.css";
import { UserContext } from "@/context/userContext";
import { BusinessContext } from "@/context/businessContext";
import { ReviewsContext } from "@/context/reviewContext";
import CustomSessionProvider from "./components/session-provider";

export const metadata: Metadata = {
  title: "Rategate",
  description: "Generated by create next app",
  icons: `${process.env.NEXT_PUBLIC_BASE_URL}/favicon.ico`,
  metadataBase: new URL(
    process.env.NEXT_PUBLIC_BASE_URL || "https://reviews.rategate.cc"
  ),
  alternates: {
    canonical: "/",
  },
  openGraph: {
    title: "Rategate",
    description: "Generated by create next app",
    url: process.env.NEXT_PUBLIC_BASE_URL || "https://reviews.rategate.cc",
    siteName: "Rategate",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <Script
          src={`https://www.googletagmanager.com/gtag/js?id=${process.env.GOOGLE_ANALYTICS_ID}`}
          strategy="afterInteractive"
        />
        <Script id="google-analytics" strategy="afterInteractive">
          {`
          window.dataLayer = window.dataLayer || [];
          function gtag(){window.dataLayer.push(arguments);}
          gtag('js', new Date());

          gtag('config', '${process.env.GOOGLE_ANALYTICS_ID}');
        `}
        </Script>
      </head>
      <body className="antialiased">
        <CustomSessionProvider>
          <UserContext>
            <BusinessContext>
              <ReviewsContext>{children}</ReviewsContext>
            </BusinessContext>
          </UserContext>
        </CustomSessionProvider>
      </body>
    </html>
  );
}
